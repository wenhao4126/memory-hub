# ============================================================
# 多智能体记忆中枢 - Python 依赖
# ============================================================
# 安装命令: pip install -r requirements.txt
# 
# 注意：Python 3.14 兼容性
# - 使用最新版本以支持 Python 3.14
# - psycopg 替代 psycopg2-binary（无需编译）
# ============================================================

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 数据库
asyncpg>=0.30.0  # 支持 Python 3.14 的最新版本
psycopg[binary]>=3.2.10  # psycopg3，支持 Python 3.14

# 数据验证
pydantic>=2.10.0  # 支持 Python 3.14
pydantic-settings>=2.6.0

# 重试机制
tenacity>=8.2.3  # 支持最新版本

# 向量嵌入（本地模型）
sentence-transformers>=3.0.0

# 向量嵌入（可选，远程 API）
httpx>=0.27.0  # 异步 HTTP 客户端，用于调用 DashScope API
dashscope>=1.20.0  # 阿里云 DashScope SDK（官方推荐）
# openai>=1.50.0
# tiktoken>=0.7.0

# 开发工具
python-dotenv>=1.0.0
httpx>=0.27.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0